<view class="grid wrap-class">
  <view class="item" wx:for="{{grids}}" wx:key="img">
    <image src="{{item.img}}" class="img" mode="widthFix" />
    <view class="text">{{item.text}}</view>
  </view>
</view>