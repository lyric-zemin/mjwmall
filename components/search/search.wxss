.search {
  display: block;
  width: 100%;
  height: 54rpx;
  line-height: 54rpx;
  text-align: center;
  color: var(--color-text-i);
  background: var(--color);
  font-size: var(--size-small);
  border-radius: 30rpx;
}